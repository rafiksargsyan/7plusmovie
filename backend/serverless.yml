service: backend
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs14.x

functions:
  hello:
    handler: app/dist/handler.hello
    events:
      - httpApi:
          path: /
          method: get
