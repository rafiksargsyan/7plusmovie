org: rsargsyan
app: backend
service: backend
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs14.x

functions:
  main:
    handler: src/main.main
    events:
      - httpApi:
          path: /
          method: get

plugins:
  - serverless-plugin-typescript

